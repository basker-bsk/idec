/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.ctfassets.net",
      "fakestoreapi.com",
      "www.apem.com",
      "images.pexels.com",
    ],
  },
  // experimental: {
  //   serverActions: true,
  // },
};

export default nextConfig;
